// Package version provides build version information for bacot.
package version

// Version is the current bacot version. Set at build time via ldflags.
var Version = "1.0.0"

// Commit is the git commit hash. Set at build time via ldflags.
var Commit = "unknown"

// BuildDate is the build date. Set at build time via ldflags.
var BuildDate = "unknown"
